# https://devenv.sh/automatic-shell-activation/
# Requires direnv with nix-direnv >= 3.0 (which provides `use devenv`).
# Run `direnv allow` once after checkout. Without direnv, just run `devenv shell`.
use devenv
